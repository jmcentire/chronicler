"""MCP server for Chronicler (stub)."""

from __future__ import annotations

import logging

logger = logging.getLogger(__name__)


def mcp_server_create(engine=None):
    """Create and configure the MCP server instance (stub)."""
    logger.info("MCP server creation stub")
    return None


async def mcp_server_run(server=None):
    """Run the MCP server (stub)."""
    logger.info("MCP server run stub")
